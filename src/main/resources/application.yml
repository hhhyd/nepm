server:
    port: 8080
    servlet:
#      context-path: /nepm
        
logging:
    level:
        # org.springframework: debug
        com.neusoft.nepm.mapper: debug   
        
spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/nepm?characterEncoding=utf-8&useSSL=false
        username: root
        password: root

        
mybatis-plus:
    mapper-locations: classpath:mapper/*.xml
    type-aliases-package: com.neusoft.nepm.po,com.neusoft.nepm.dto
    
global-data:
    province-number: 34
    city-number: 106    
    
        
